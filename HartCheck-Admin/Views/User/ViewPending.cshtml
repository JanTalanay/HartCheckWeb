@using HartCheck_Admin.Models
@model IEnumerable<User>

@{
    ViewData["Title"] = " Doctors";
}
<h1>@ViewData["Title"]</h1>

<br /><br />
@if (Model.Count() < 1)
{
	<div class="alert alert-info">
		No Doctor records found.
	</div>
}
else
{
	<div id="role1">
		<table id="User" class="table table-striped table-hover">
		<thead>
			<tr>
				<th>First Name</th>
				<th>Last Name</th>
				<th>Email Address</th>
				<th>Phone Number</th>
				<th>Birthdate</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				@if (item.role == 1)
				{
					<tr>
						<td>@item.firstName</td>
						<td>@item.lastName</td>
						<td>@item.email</td>
						<td>@item.phoneNumber</td>
						<td>@item.birthdate</td>
						<td>
							<a asp-controller="User" asp-action="Approve" asp-route-id="@item.usersID" data-user-id="@item.usersID" id="approve-button" class="btn btn-sm btn-outline-success">Approve</a>
                            <a asp-controller="User" asp-action="Deny" asp-route-id="@item.usersID" class="btn btn-sm btn-outline-danger">Deny</a>
                            
							<a asp-controller="User" asp-action="View" asp-route-id="@item.usersID"
								class="btn btn-info btn-sm">
								<i class="fas fa-eye"></i>
								View
							</a>
							<button class="btn btn-danger btn-sm deleteBtn" data-id="@item.usersID">
								<i class="fas fa-trash"></i>
								Delete
							</button>           
						</td>
					</tr>
				}
			}
		</tbody>
		</table>

		<div class="button-box  btn-group viewbutton" >
			<a asp-controller="User" asp-action="ViewApproved" class="btn btn-outline-info button-link"  id="approve-button"  > View Approved </a>
            <a asp-controller="User" asp-action="ViewPending" class="btn btn-outline-info button-link"  id="loadViewPendingButton"  > View Doctors </a>
            <a asp-controller="User" asp-action="ViewDenied" class="btn btn-outline-danger button-link"  id="loadViewDeniedButton"  >View Denied</a>
         </div>
	</div>
}

<form id="DeleteForm" asp-controller="User" asp-action="Delete" method="post">
	<input type="hidden" id="HiddenUserID" name="Id" value="" /> 
</form>

@{
    var viewPendingUrl = Url.Action("ViewPending", "User");
}

@section Scripts {
	<script>
		$(document).ready( function () {
			 $('#User').DataTable();
				} );
		$('.deleteBtn').click(function(e) {
            swal({
                title: "Are you sure?",
                text: "Are you sure you want to delete this record?",
                icon: "warning",
                buttons: true,
                dangerMode: true
            }).then((confirm) => {
                if (confirm) {
                    var btn = $(this);
                    var id = btn.data("id");
                    $("#HiddenUserID").val(id);
                    $('#DeleteForm').submit();
				}
			});
		});

		 $(document).ready(function () {
        $(".approve-button").click(function () {
            var usersID = $(this).data("data-user-id"); 
            $.ajax({
                url: "@Url.Action("ApproveDoctor", "User")",
                type: "POST",
                data: { usersID: usersID },
                success: function () {
                    location.reload();
                } 
            });
        });
    });
	</script>
}
